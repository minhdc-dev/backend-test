const outputFramesDir = '/tmp/video/frames';
const tempDownloadDir = '/tmp/download';
const tempUploadDir = '/tmp/upload';
const intervalSeconds = 10;
const limitSizeUpload = 15 * 1024 * 1024 * 1024; // 15GB

export {
  outputFramesDir,
  tempDownloadDir,
  tempUploadDir,
  intervalSeconds,
  limitSizeUpload,
};
